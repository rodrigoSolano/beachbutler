// @ts-check
import { Box, styled } from '@mui/material'

const OnboardingSeparator = styled(Box)(({ theme }) => ({
  width: '100%',
  height: 1,
  backgroundColor: theme.palette.common.white,
}))

export default OnboardingSeparator
